port: 8000

timer:
    runEvery: 1h        
    expire: 168h   

# db:
#     url: postgres://roxy:roxy-int-secret@localhost:5433/asr?sslmode=disable     

filer:
    # url: localhost:9000
    user: roxy
    # key: roxy-int-secret
    bucket: asr    
